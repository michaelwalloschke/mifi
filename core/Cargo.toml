[package]
name = "core"
version = "0.1.0"
edition.workspace = true

[lib]
name = "mifi_core"
path = "src/lib.rs"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
rusqlite_migration = "1.3"

[dev-dependencies]
tempfile = "3"
