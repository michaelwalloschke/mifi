# Apply for scalable-cli beta

Type: task
Status: open

## Question

Get mifi access to the official Scalable Capital CLI (github.com/ScalableCapital/scalable-cli) — the primary depot/Verrechnungskonto data path per [Aggregator selection](05-aggregator-selection.md). The CLI is beta and allowlist-gated; long lead time, so apply now. HITL: Michael applies via the process described in the repo (see [research summary](../assets/05-aggregator-selection.md) for links). Answer records: allowlist granted yes/no, OAuth device-code flow verified against the live account, which commands/fields actually return (`--json` shape for holdings + transactions), and token storage location (feeds [Security model](12-security-model.md)). If access is denied or stalls, the fallback baseline stays official CSV export.
